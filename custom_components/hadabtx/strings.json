{
  "config": {
    "step": {
      "user": {
        "title": "Connect to odrDAB TX Integration",
        "data": {
          "host": "Host (IP address)",
          "username": "Username",
          "password": "Password"
        }
      }
    },
    "error": {
      "cannot_connect": "Could not connect to the modulator. Check the host and credentials."
    },
    "abort": {
      "already_configured": "This modulator is already configured."
    }
  },
"entity": {
    "sensor": {
      "frequency": { "name": "Frequency" },
      "dab_block": { "name": "DAB block" },
      "amplitude": { "name": "Amplitude" },
      "dac_current": { "name": "DAC current" },
      "remote_ip": { "name": "Remote IP" },
      "remote_port": { "name": "Remote port" },
      "connection_state": { "name": "Network connection status" },
      "rf_frontend_status": { "name": "RF frontend status" },
      "ifft_overflow": { "name": "iFFT overflow" },
      "fir_clipped": { "name": "FIR clipped" },
      "tii_main_id": { "name": "TII Main Id" },
      "tii_sub_id": { "name": "TII Sub Id" }
    },
    "binary_sensor": {
      "sram_detected": { "name": "SPI SRAM buffers detected" },
      "broadcasting": { "name": "Broadcasting" }
    },
    "switch": {
      "rf_frontend_switch": { "name": "RF frontend" },
      "eti_socket_switch": { "name": "ETI socket" }
    },
    "number": {
      "amplitude_set": { "name": "Amplitude (set)" },
      "dac_current_set": { "name": "DAC current (set)" },
      "remote_port_set": { "name": "Remote port (set)" },
      "frequency_set": { "name": "Frequency Hz (set)" },
      "tii_main_id_set": { "name": "TII Main Id (set)" },
      "tii_sub_id_set": { "name": "TII Sub Id (set)" }
    },
    "select": {
      "dab_block_set": { "name": "DAB block (set)" },
      "connection_mode_set": { "name": "Connection mode (set)" }
    },
    "text": {
      "remote_ip_set": { "name": "Remote IP (set)" }
    },
    "button": {
      "apply_changes": { "name": "Apply changes" }
    }
  },
  "services": {
    "set_config": {
      "name": "Set config",
      "description": "Change one or more settings on a DAB modulator."
    }
  }
}
